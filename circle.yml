general:
  branches:
    only:
      - master
machine:
  python:
    version: 3.5.2
dependencies:
  override:
    - pip install -r requirements.txt
test:
  override:
    - exit 0 # We don't use CI to test
compile:
  override:
    - mkdocs build
deployment:
  production:
    branch: master
    commands:
    - git config user.name "Tristian Forcum"
    - git config user.email tslimshady99@hotmail.com
    - mkdocs gh-deploy
experimental:
  notify:
    branches:
      only:
        - master
