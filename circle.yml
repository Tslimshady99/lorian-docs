machine:
  python:
    version: 2.7.11
general:
  branches:
    ignore:
      - gh-pages
dependencies:
  post:
    - pip install --upgrade pip
    - pip install mkdocs
    - pip install mkdocs-material
test:
  override:
    - mkdocs build --clean --verbose
deployment:
  production:
    branch: master
    commands:
      - mkdocs gh-deploy
