general:
  branches:
    only:
      - master
machine:
  python:
    version: 3.5.2
test:
  override:
    - exit 0
compile:
  override:
    - pip install -r requirements.txt
    - mkdocs build
deployment:
  master:
    branch: master
    commands:
      - git config user.email Tslimshady99@hotmail.com
      - mkdocs gh-deploy
experimental:
  notify:
    branches:
      only:
        - master
