machine:
  python:
    version: 2.7.11
general:
  branches:
    ignore:
      - gh-pages
dependencies:
  post:
    - pip install --upgrade pip
test:
  override:
    - mkdocs build --clean --verbose
deployment:
  production:
    branch: docs
    commands:
      - mkdocs gh-deploy
